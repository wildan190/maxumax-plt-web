import './bootstrap';

// Alpine.js is loaded via CDN in app.blade.php for simplicity in this transition phase
// If you want to bundle it, uncomment the lines below:
// import Alpine from 'alpinejs';
// window.Alpine = Alpine;
// Alpine.start();
